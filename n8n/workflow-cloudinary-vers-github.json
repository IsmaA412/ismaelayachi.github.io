{
  "name": "Cloudinary vers portfolio (photos.json)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "cloudinary-photo",
        "responseMode": "responseNode",
        "options": {
          "rawBody": true
        }
      },
      "id": "webhook-cloudinary",
      "name": "Webhook Cloudinary",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        -220,
        0
      ],
      "webhookId": "cloudinary-photo"
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "a1",
              "name": "proprietaire",
              "value": "IsmaA412",
              "type": "string"
            },
            {
              "id": "a2",
              "name": "depot",
              "value": "ismaelayachi.github.io",
              "type": "string"
            },
            {
              "id": "a3",
              "name": "branche",
              "value": "main",
              "type": "string"
            },
            {
              "id": "a4",
              "name": "chemin",
              "value": "photos.json",
              "type": "string"
            },
            {
              "id": "a5",
              "name": "tagRequis",
              "value": "portfolio",
              "type": "string"
            },
            {
              "id": "cfg6",
              "name": "maxSuppressions",
              "value": 5,
              "type": "number"
            }
          ]
        },
        "options": {}
      },
      "id": "configuration",
      "name": "Configuration",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "// Vérifie que la requête vient bien de Cloudinary.\n//\n// Ce webhook est une URL publique qui déclenche un commit sur le dépôt : sans\n// cette vérification, n'importe qui connaissant l'adresse pourrait publier une\n// image sur le site. Cloudinary signe chaque notification, on recalcule la\n// signature et on refuse tout ce qui ne correspond pas.\n//\n// Formule Cloudinary : SHA1(corps_brut + timestamp + api_secret)\n//\n// SHA-1 est implémenté ici en JavaScript pur, à dessein. Le bac à sable des\n// nœuds Code n'expose ni le module `crypto` de Node (il faudrait autoriser\n// NODE_FUNCTION_ALLOW_BUILTIN) ni l'API Web Crypto globale. Cette version ne\n// dépend d'aucune configuration de l'instance.\n\nfunction sha1Hex(octets) {\n  const n = octets.length;\n  // Bourrage : un bit à 1, des zéros, puis la longueur en bits sur 64 bits.\n  const totalBlocs = ((n + 8) >> 6) + 1;\n  const mots = new Int32Array(totalBlocs * 16);\n  for (let i = 0; i < n; i++) mots[i >> 2] |= octets[i] << (24 - (i % 4) * 8);\n  mots[n >> 2] |= 0x80 << (24 - (n % 4) * 8);\n  mots[totalBlocs * 16 - 1] = n * 8;\n  mots[totalBlocs * 16 - 2] = Math.floor((n * 8) / 4294967296);\n\n  let h0 = 0x67452301,\n    h1 = 0xefcdab89,\n    h2 = 0x98badcfe,\n    h3 = 0x10325476,\n    h4 = 0xc3d2e1f0;\n  const w = new Int32Array(80);\n  const rot = (v, s) => (v << s) | (v >>> (32 - s));\n\n  for (let bloc = 0; bloc < totalBlocs; bloc++) {\n    for (let i = 0; i < 16; i++) w[i] = mots[bloc * 16 + i];\n    for (let i = 16; i < 80; i++)\n      w[i] = rot(w[i - 3] ^ w[i - 8] ^ w[i - 14] ^ w[i - 16], 1);\n    let a = h0, b = h1, c = h2, d = h3, e = h4;\n    for (let i = 0; i < 80; i++) {\n      let f, k;\n      if (i < 20) { f = (b & c) | (~b & d); k = 0x5a827999; }\n      else if (i < 40) { f = b ^ c ^ d; k = 0x6ed9eba1; }\n      else if (i < 60) { f = (b & c) | (b & d) | (c & d); k = 0x8f1bbcdc; }\n      else { f = b ^ c ^ d; k = 0xca62c1d6; }\n      const t = (rot(a, 5) + f + e + k + w[i]) | 0;\n      e = d; d = c; c = rot(b, 30); b = a; a = t;\n    }\n    h0 = (h0 + a) | 0; h1 = (h1 + b) | 0; h2 = (h2 + c) | 0;\n    h3 = (h3 + d) | 0; h4 = (h4 + e) | 0;\n  }\n  return [h0, h1, h2, h3, h4]\n    .map((x) => (x >>> 0).toString(16).padStart(8, '0'))\n    .join('');\n}\n\nconst secret = $env.CLOUDINARY_API_SECRET;\nif (!secret) {\n  throw new Error(\n    \"Variable d'environnement CLOUDINARY_API_SECRET absente côté n8n. \" +\n      'Relancer n8n avec CLOUDINARY_API_SECRET=… et ' +\n      'N8N_BLOCK_ENV_ACCESS_IN_NODE=false.'\n  );\n}\n\nconst entree = $('Webhook Cloudinary').first();\nconst headers = entree.json.headers || {};\nconst signatureRecue = headers['x-cld-signature'];\nconst timestamp = headers['x-cld-timestamp'];\n\nif (!signatureRecue || !timestamp) {\n  throw new Error('Requête non signée : en-têtes Cloudinary manquants.');\n}\n\n// Le corps doit être repris tel quel, octet pour octet : ré-encoder l'objet\n// JSON analysé changerait l'ordre des clés ou les espaces, et la signature ne\n// correspondrait plus. L'option « Raw Body » du nœud Webhook place ce corps\n// d'origine dans les données binaires ; `json.body` n'en est que la version\n// analysée, inutilisable pour la vérification.\nlet corpsBrut;\nif (entree.binary && entree.binary.data && entree.binary.data.data) {\n  corpsBrut = Buffer.from(entree.binary.data.data, 'base64').toString('utf8');\n} else if (typeof entree.json.body === 'string') {\n  corpsBrut = entree.json.body;\n} else {\n  throw new Error(\n    'Corps brut introuvable : activer « Raw Body » sur le nœud Webhook.'\n  );\n}\n\nconst attendue = sha1Hex(Buffer.from(corpsBrut + timestamp + secret, 'utf8'));\n\n// Comparaison à durée constante : une comparaison classique s'arrête au premier\n// caractère faux, ce qui laisse deviner la signature essai par essai.\nconst recue = String(signatureRecue);\nlet ecart = attendue.length ^ recue.length;\nfor (let i = 0; i < attendue.length; i++) {\n  ecart |= attendue.charCodeAt(i) ^ recue.charCodeAt(i);\n}\nif (ecart !== 0) {\n  throw new Error('Signature Cloudinary invalide : requête rejetée.');\n}\n\n// Rejette les notifications trop anciennes (rejeu d'une requête interceptée).\nconst ageSecondes = Math.floor(Date.now() / 1000) - Number(timestamp);\nif (ageSecondes > 3600) {\n  throw new Error(`Notification trop ancienne (${ageSecondes} s) : rejetée.`);\n}\n\nreturn [{ json: JSON.parse(corpsBrut) }];\n"
      },
      "id": "verif-signature",
      "name": "Vérifier la signature Cloudinary",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        0
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "version": 2,
            "typeValidation": "loose"
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "c1",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              },
              "leftValue": "={{ ($json.notification_type === 'upload' && $json.resource_type === 'image' && Array.isArray($json.tags) && $json.tags.includes($('Configuration').first().json.tagRequis)) || $json.notification_type === 'delete' }}",
              "rightValue": ""
            }
          ]
        },
        "options": {}
      },
      "id": "filtre",
      "name": "Notification pertinente ?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        440,
        0
      ]
    },
    {
      "parameters": {
        "url": "=https://api.github.com/repos/{{ $('Configuration').first().json.proprietaire }}/{{ $('Configuration').first().json.depot }}/contents/{{ $('Configuration').first().json.chemin }}?ref={{ $('Configuration').first().json.branche }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Accept",
              "value": "application/vnd.github+json"
            },
            {
              "name": "User-Agent",
              "value": "n8n-portfolio-sync"
            }
          ]
        },
        "options": {}
      },
      "id": "lire-json",
      "name": "Lire photos.json",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        660,
        -100
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "sY96p7w7tJq3cU4n",
          "name": "Header Auth account"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// Applique la notification Cloudinary à photos.json : ajout ou retrait.\n//\n// photos.json est la seule source de la galerie. Ce nœud le lit, le modifie en\n// mémoire et renvoie le contenu à commiter ; index.html n'est jamais touché.\n\nconst config = $('Configuration').first().json;\nconst notification = $('Vérifier la signature Cloudinary').first().json;\nconst fichier = $('Lire photos.json').first().json;\n\n// L'API GitHub renvoie le contenu encodé en base64.\nconst actuel = JSON.parse(\n  Buffer.from(fichier.content, 'base64').toString('utf8')\n);\nif (!Array.isArray(actuel.photos)) {\n  throw new Error(\"photos.json est illisible : « photos » n'est pas une liste.\");\n}\n\nconst ignorer = (raison) => [{ json: { ignore: true, raison } }];\n\n// Prépare la sortie destinée au nœud de commit.\nconst aCommiter = (message, resume) => {\n  const contenu = JSON.stringify(actuel, null, 2) + '\\n';\n  return [\n    {\n      json: {\n        ignore: false,\n        message,\n        total: actuel.photos.length,\n        sha: fichier.sha,\n        contenuBase64: Buffer.from(contenu, 'utf8').toString('base64'),\n        urlContenu: `https://api.github.com/repos/${config.proprietaire}/${config.depot}/contents/${config.chemin}`,\n        branche: config.branche,\n        ...resume,\n      },\n    },\n  ];\n};\n\n// ---------------------------------------------------------------- SUPPRESSION\nif (notification.notification_type === 'delete') {\n  const ressources = Array.isArray(notification.resources)\n    ? notification.resources\n    : [];\n\n  // Une notification de suppression ne porte pas de tags : impossible d'y\n  // vérifier que la photo appartenait au portfolio. Sa présence dans\n  // photos.json en tient lieu — ce qui n'y figure pas ne peut pas en sortir.\n  // Une suppression groupée en vise plusieurs à la fois, d'où la liste.\n  const idsSupprimes = new Set(\n    ressources\n      .filter((r) => r && (!r.resource_type || r.resource_type === 'image'))\n      .map((r) => r.public_id)\n      .filter((id) => typeof id === 'string' && id.length > 0)\n  );\n\n  const retirees = actuel.photos\n    .filter((p) => idsSupprimes.has(p.id))\n    .map((p) => p.id);\n\n  if (retirees.length === 0) {\n    return ignorer(\n      'Aucune des photos supprimées ne figurait dans photos.json.'\n    );\n  }\n\n  // Garde-fou : une suppression groupée malencontreuse dans la médiathèque\n  // Cloudinary viderait la galerie sans confirmation. Au-delà de la limite,\n  // l'exécution s'arrête en erreur et rien n'est commité.\n  const limite = Number(config.maxSuppressions);\n  if (Number.isFinite(limite) && limite > 0 && retirees.length > limite) {\n    throw new Error(\n      `${retirees.length} photos seraient retirées en une fois, au-delà de la ` +\n        `limite de ${limite}. Rien n'a été commité. Relever « maxSuppressions » ` +\n        'dans le nœud Configuration, ou modifier photos.json à la main.'\n    );\n  }\n\n  actuel.photos = actuel.photos.filter((p) => !idsSupprimes.has(p.id));\n\n  return aCommiter(\n    retirees.length === 1\n      ? `Retrait de la photo ${retirees[0]}`\n      : `Retrait de ${retirees.length} photos`,\n    { action: 'supprimee', photos: retirees }\n  );\n}\n\n// --------------------------------------------------------------------- AJOUT\n// Cloudinary place les métadonnées saisies dans la médiathèque ici. Le texte\n// alternatif décrit la photo pour les lecteurs d'écran et les moteurs de\n// recherche : à renseigner dans Cloudinary, champ « alt ».\nconst contexte = (notification.context && notification.context.custom) || {};\n\n// Couleur dominante, utilisée comme aplat pendant le chargement de la vignette.\n// Cloudinary la fournit sous la forme [[\"#7c6c55\", 34.1], ...], triée par\n// dominance, quand l'option « Retrieve predominant colors and histogram » est\n// cochée sur le preset d'upload. Sinon la galerie retombe sur un gris neutre.\nlet couleur = null;\nif (Array.isArray(notification.colors) && notification.colors.length > 0) {\n  const premiere = notification.colors[0];\n  const brut = Array.isArray(premiere) ? premiere[0] : premiere;\n  // Cette valeur finit telle quelle dans une propriété CSS de la page. On\n  // refuse tout ce qui n'est pas un code hexadécimal : mieux vaut le gris par\n  // défaut qu'un aplat invisible ou une déclaration CSS cassée.\n  if (typeof brut === 'string' && /^#[0-9a-f]{6}$/i.test(brut.trim())) {\n    couleur = brut.trim().toLowerCase();\n  }\n}\n\nconst nouvelle = {\n  id: notification.public_id,\n  version: String(notification.version),\n  format: notification.format,\n  width: notification.width,\n  height: notification.height,\n  alt: contexte.alt || contexte.caption || '',\n};\nif (couleur) {\n  nouvelle.color = couleur;\n}\n\n// Cloudinary peut renvoyer plusieurs fois la même notification. Sans ce filtre,\n// la photo apparaîtrait en double dans la galerie.\nif (actuel.photos.some((p) => p.id === nouvelle.id)) {\n  return ignorer(`Photo « ${nouvelle.id} » déjà présente dans photos.json.`);\n}\n\n// Les nouveautés en tête de galerie.\nactuel.photos.unshift(nouvelle);\n\nreturn aCommiter(`Ajout de la photo ${nouvelle.id}`, {\n  action: 'publiee',\n  photo: nouvelle,\n  photos: [nouvelle.id],\n});\n"
      },
      "id": "construire",
      "name": "Modifier photos.json",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        880,
        -100
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "version": 2,
            "typeValidation": "loose"
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "d1",
              "operator": {
                "type": "boolean",
                "operation": "false",
                "singleValue": true
              },
              "leftValue": "={{ $json.ignore }}",
              "rightValue": ""
            }
          ]
        },
        "options": {}
      },
      "id": "filtre-doublon",
      "name": "Changement à commiter ?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        1100,
        -100
      ]
    },
    {
      "parameters": {
        "method": "PUT",
        "url": "={{ $json.urlContenu }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Accept",
              "value": "application/vnd.github+json"
            },
            {
              "name": "User-Agent",
              "value": "n8n-portfolio-sync"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ message: $json.message, content: $json.contenuBase64, sha: $json.sha, branch: $json.branche }) }}",
        "options": {}
      },
      "id": "commit",
      "name": "Commit photos.json",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1320,
        -180
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "sY96p7w7tJq3cU4n",
          "name": "Header Auth account"
        }
      }
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ statut: $('Modifier photos.json').first().json.action, photos: $('Modifier photos.json').first().json.photos, total: $('Modifier photos.json').first().json.total }) }}",
        "options": {}
      },
      "id": "reponse-ok",
      "name": "Réponse : publiée",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1540,
        -180
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ statut: 'ignoree', raison: $json.raison || 'La photo ne porte pas le tag requis.' }) }}",
        "options": {}
      },
      "id": "reponse-ignore",
      "name": "Réponse : ignorée",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1320,
        120
      ]
    }
  ],
  "connections": {
    "Webhook Cloudinary": {
      "main": [
        [
          {
            "node": "Configuration",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Configuration": {
      "main": [
        [
          {
            "node": "Vérifier la signature Cloudinary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Vérifier la signature Cloudinary": {
      "main": [
        [
          {
            "node": "Notification pertinente ?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Notification pertinente ?": {
      "main": [
        [
          {
            "node": "Lire photos.json",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Réponse : ignorée",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Lire photos.json": {
      "main": [
        [
          {
            "node": "Modifier photos.json",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Modifier photos.json": {
      "main": [
        [
          {
            "node": "Changement à commiter ?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Changement à commiter ?": {
      "main": [
        [
          {
            "node": "Commit photos.json",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Réponse : ignorée",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Commit photos.json": {
      "main": [
        [
          {
            "node": "Réponse : publiée",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "pinData": {}
}